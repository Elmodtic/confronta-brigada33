# --- Claude Code (config local, no compartir) ---
.claude/settings.local.json

# --- Node.js (backend/) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.*
!.env.example
*.log

# --- Android / Kotlin ---
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
/captures/
android/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/
out/
.navigation/
release/
crashlytics.properties
crashlytics-build.properties
fabric.properties

# Documentacion interna (no se sube; ver docs/CONTEXTO_TECNICO.md)
CLAUDE.md

# Notas locales con datos sensibles (nunca se sube)
Credenciales.txt

# --- Claves privadas TLS (nunca se suben) ---
certs/*.key
*.key
*.pem
*.pfx

# --- Firma del APK de release (NUNCA se sube: si se filtra, cualquiera
#     puede publicar actualizaciones falsas de la app) ---
android/keystore/
android/keystore.properties
*.jks
*.keystore

# Direccion temporal del tunel (cambia en cada arranque)
url_tunel.txt
tunel.log

arranque.log

# --- Respaldos de la base: llevan cedulas y hashes de contrasenas ---
db/respaldos/
*.sql.bak

# --- Copias de trabajo (no aportan nada al repo) ---
*.bak
*.bak-*
*.bak[0-9]
android/.backup-ui/
